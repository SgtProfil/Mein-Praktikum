var resultadd = add (1, 2);
  var resultsuptract = suptract (1, 2);
    var resultmultiply = multiply (1, 2);
      var resultdivide = divide (1, 2);
        console.log(resultadd);
      console.log(resultsuptract);
    console.log(resultmultiply);
  console.log(resultdivide);

function multiply(wert1, wert2){
  return (wert1*wert2);
    }function add(wert1, wert2){
      return (wert1+wert2)
        }function divide(wert1, wert2){
          return (wert1/wert2);
            }function suptract(wert1, wert2){
              return (wert1-wert2)
}
